---

- name: auditd reload rules
  ansible.builtin.command:
    cmd: augenrules --load
  changed_when: true

- name: auditd restart
  ansible.builtin.command:
    cmd: service auditd restart
  changed_when: true
