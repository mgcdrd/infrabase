---

- name: auditd reload rules
  ansible.builtin.command:
    cmd: augenrules --load
  changed_when: true

- name: auditd restart
  ansible.builtin.systemd_service:
    name:  auditd
    state: restarted
