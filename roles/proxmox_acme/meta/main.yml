---
galaxy_info:
  author: mgcdrd
  description: Manage ACME certificates for ProxMox VE nodes' own web UI (pveproxy) — accounts, DNS challenge plugins, and cert ordering — via the PVE API
  role_name: proxmox_acme

  license: GPL-3.0-or-later
  min_ansible_version: '2.14'

  galaxy_tags:
    - mgcdrd

collections:
  - community.proxmox
